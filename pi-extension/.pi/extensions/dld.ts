// Auto-discovered by Pi when running `pi` from this repo root.
// Real implementation lives in ../../src/index.ts so the repo layout
// stays clean. Pi's jiti loader follows the relative import.
export { default } from "../../src/index.ts";
